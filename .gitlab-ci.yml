before_script:
  - apt-get update -qq && apt-get install -y -qq libhdf5-dev

install:
    image: python:3.7.0
    script:
        - pip install .

test:
    image: python:3.7.0
    script:
        - km3pipe test
